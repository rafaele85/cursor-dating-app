`function $name($first = $default, $rest) $body` where {
  register_diagnostic(
    span = $first,
    message = "Default parameters are not allowed. Use explicit arguments or an options object.",
    severity = "error"
  )
}
