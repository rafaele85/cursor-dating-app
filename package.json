{
  "name": "cursor-dating-app",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "lint": "biome check src test/run-biome-rules-tests.mjs test/unit vitest.config.ts biome.json apps/web",
    "lint:fix": "biome check --write src test/run-biome-rules-tests.mjs test/unit vitest.config.ts biome.json apps/web",
    "lint:all": "npm run lint && npm run lint:duplication && npm run lint:unused-exports",
    "lint:duplication": "jscpd src/ apps/web",
    "lint:unused-exports": "ts-prune && npm run lint:unused-exports --workspaces --if-present",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit",
    "typecheck:apps": "npm run typecheck --workspaces --if-present",
    "test": "npm run test:all",
    "test:all": "npm run test:biome-rules && npm run test:unit && npm run test --workspaces --if-present",
    "test:biome-rules": "node test/run-biome-rules-tests.mjs",
    "test:unit": "vitest run",
    "test:coverage": "vitest run --coverage",
    "quality-gates": "npm run lint:all && npm run typecheck && npm run typecheck:apps && npm run test:all"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.18",
    "@biomejs/biome": "^2.3.14",
    "jscpd": "^4.0.8",
    "ts-prune": "^0.10.3",
    "typescript": "~5.7.0",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=20"
  }
}
